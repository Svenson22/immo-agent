# Gmail API integration
google-api-python-client>=2.170,<3
google-auth>=2.30
google-auth-oauthlib>=1.2
google-auth-httplib2>=0.2.0

# HTML parsing for email content
beautifulsoup4==4.13.4
lxml>=5
html5lib>=1.1
python-dateutil>=2.9

# Config (optional)
python-dotenv>=1.0

# --- Later / optional ---
# AI and LangChain (for classification/ranking in later step)
# langchain>=0.3
# anthropic>=0.60

# IMAP fallback (only needed if not using Gmail API)
# imap-tools>=1.0